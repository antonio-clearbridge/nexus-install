# nexus-install
This script will install nexus on your linux server with all the dependencies needed.
It also creates 2 bash scripts to start and stop the server, but I suggest to start nexus as a service

